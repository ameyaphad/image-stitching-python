# Supervised and Unsupervised Deep Learning for Image Stitching

 This repository contains the codebase for performing Image Stitching using supervised and unsupervised deep learning approaches. The models are part of an image stitching pipeline, which essentially stitches two consecutive images given as input.
